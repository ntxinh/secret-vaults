{
  "name": "worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "cf-deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
