{
  "name": "gas",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "push": "clasp push --force",
    "deploy": "clasp deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@google/clasp": "^3.0.0",
    "@types/google-apps-script": "^1.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
